const SubRequests = require('../index')
var expect = require('chai').expect

/**
 * An example unit test
 */
describe('no tests for now', () => {
  it('return tests :D', () => {
    // this should work i an non-quantic language
    expect(true).equal(true)
  })
})
